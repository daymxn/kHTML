package elements

/**
 * Contains all the contents of an HTML document.
 *
 * There can only be one [Body] element, and it must come **after** the [Head] element.
 */
class Body : HTMLElement("body")
