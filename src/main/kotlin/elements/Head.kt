package elements

/**
 * Contains metadata about the HTML document.
 *
 * There can only be one [Head] element, and it must be the first child of the [HTML] element.
 */
class Head: HTMLElement("head")
